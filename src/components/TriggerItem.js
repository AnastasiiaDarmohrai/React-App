import React from 'react';

const TriggerItem = ({text}) => {
    return (
        <div>
            <div className="block">
                    {text}
            </div>
        </div>
    );
};

export default TriggerItem;
